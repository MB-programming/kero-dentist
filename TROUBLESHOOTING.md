# دليل حل المشاكل - Kero Dentist

## 🔴 المشاكل الشائعة والحلول

### 1️⃣ إضافة الأدمن لا تعمل (Add Admin Not Working)

**السبب:**
- قاعدة البيانات `u186120816_kero_dentist` فارغة أو جدول `admin_users` غير موجود

**الحل:**

#### الطريقة الأولى: استخدام أداة الفحص التلقائي ✅ (الأسهل)
```
افتح المتصفح واذهب إلى:
http://your-domain.com/check_and_fix_database.php
```

ستقوم الأداة تلقائياً بـ:
- فحص جميع الجداول
- إنشاء مستخدم أدمن افتراضي إذا لم يكن موجوداً
- إظهار الإحصائيات والمشاكل

**بيانات الدخول الافتراضية:**
```
اسم المستخدم: admin
كلمة المرور: admin123
```

#### الطريقة الثانية: استيراد قاعدة البيانات الكاملة

1. افتح **phpMyAdmin**
2. اختر قاعدة البيانات: `u186120816_kero_dentist`
3. اضغط على **"استيراد" (Import)**
4. اختر الملف: `complete_database.sql`
5. اضغط **"تنفيذ" (Execute)**

#### الطريقة الثالثة: عبر سطر الأوامر
```bash
mysql -u u186120816_kero_dentist -p u186120816_kero_dentist < complete_database.sql
# أدخل كلمة المرور: 9Ea$eZnZ#
```

---

### 2️⃣ السلايدرز لا تظهر (Sliders Not Showing)

**السبب:**
- جدول `sliders` فارغ أو غير موجود في قاعدة البيانات

**الحل:**

#### الحل الأول: إضافة سلايدر من لوحة التحكم
1. سجل دخول للوحة التحكم: `admin/login.php`
2. اذهب إلى: **إدارة السلايدر** (Sliders)
3. اضغط **"إضافة سلايدر جديد"**
4. املأ البيانات وارفع الصورة
5. احفظ

#### الحل الثاني: استيراد البيانات التجريبية
قم باستيراد ملف `complete_database.sql` الذي يحتوي على:
- ✅ 6 باقات تجريبية
- ✅ 4 خدمات تجريبية
- ✅ 3 أطباء تجريبيين
- ✅ 6 تقييمات تجريبية
- ✅ بيانات افتراضية كاملة

---

### 3️⃣ خطأ: "Database connection failed"

**السبب:**
- بيانات الاتصال خاطئة في `includes/config.php`

**الحل:**

تأكد من بيانات الاتصال في ملف `includes/config.php`:

```php
// Database Configuration - Production
define('DB_HOST', 'localhost');
define('DB_USER', 'u186120816_kero_dentist');
define('DB_PASS', '9Ea$eZnZ#');
define('DB_NAME', 'u186120816_kero_dentist');
```

تأكد من:
1. ✅ اسم المستخدم صحيح
2. ✅ كلمة المرور صحيحة
3. ✅ قاعدة البيانات موجودة على السيرفر
4. ✅ المستخدم لديه صلاحيات الوصول للقاعدة

---

### 4️⃣ أزرار الواتساب لا تعمل

**السبب:**
- تم إصلاحه في آخر تحديث

**التحقق:**
تأكد من أنك قمت بتحديث الملفات:
- ✅ `admin/bookings.php` - تم إصلاح خطأ JavaScript
- ✅ `api/get-booking.php` - تم إنشاؤه
- ✅ `api/submit-booking.php` - يستخدم قاعدة البيانات الموحدة

---

### 5️⃣ الحجوزات لا تُحفظ

**السبب:**
- جدول `bookings` غير موجود أو قاعدة البيانات غير متصلة

**الحل:**
1. افتح: `http://your-domain.com/check_and_fix_database.php`
2. تحقق من وجود جدول `bookings`
3. إذا كان مفقوداً، استورد `complete_database.sql`

---

## 🛠️ أدوات الصيانة

### أداة فحص قاعدة البيانات
```
http://your-domain.com/check_and_fix_database.php
```

تقوم بـ:
- ✅ فحص الاتصال بقاعدة البيانات
- ✅ عرض جميع الجداول الموجودة والمفقودة
- ✅ إحصائيات عدد السجلات في كل جدول
- ✅ إنشاء مستخدم أدمن افتراضي تلقائياً
- ✅ إرشادات واضحة لحل المشاكل

---

## 📊 هيكل قاعدة البيانات

### الجداول المطلوبة (13 جدول):
1. ✅ `admin_users` - مستخدمي لوحة التحكم
2. ✅ `services` - الخدمات
3. ✅ `packages` - الباقات
4. ✅ `bookings` - الحجوزات (مع كشف شركة الهاتف)
5. ✅ `booking_history` - سجل تغييرات الحجوزات
6. ✅ `booking_stats` - إحصائيات الحجوزات
7. ✅ `blog_posts` - المقالات
8. ✅ `reviews` - آراء العملاء
9. ✅ `settings` - إعدادات الموقع
10. ✅ `sliders` - السلايدر/البانرات
11. ✅ `email_templates` - قوالب البريد الإلكتروني
12. ✅ `menu_items` - عناصر القائمة
13. ✅ `doctors` - الأطباء

---

## 🔐 بيانات الدخول الافتراضية

### لوحة التحكم
```
URL: http://your-domain.com/admin/login.php
Username: admin
Password: admin123
```

⚠️ **مهم جداً:** قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول!

---

## 📱 كشف شركات الاتصالات المصرية

النظام يكتشف تلقائياً شركة الهاتف من رقم الموبايل:

- **Vodafone:** 010, 011
- **Orange:** 012
- **Etisalat:** 011
- **WE:** 015

---

## 🔄 ترحيل البيانات من قاعدة بيانات قديمة

إذا كنت تريد نقل البيانات من قاعدة بيانات قديمة:

### الطريقة 1: عبر phpMyAdmin
1. افتح قاعدة البيانات القديمة
2. اختر "تصدير" (Export)
3. اختر جميع الجداول
4. حمّل ملف SQL
5. افتح قاعدة البيانات الجديدة `u186120816_kero_dentist`
6. اختر "استيراد" (Import)
7. ارفع ملف SQL المصدّر

### الطريقة 2: عبر سطر الأوامر
```bash
# تصدير من القديمة
mysqldump -u old_user -p old_database > backup.sql

# استيراد للجديدة
mysql -u u186120816_kero_dentist -p u186120816_kero_dentist < backup.sql
```

---

## 📞 الدعم

إذا استمرت المشاكل:
1. ✅ تحقق من ملف: `check_and_fix_database.php`
2. ✅ راجع سجلات الأخطاء (error logs)
3. ✅ تأكد من صلاحيات الملفات والمجلدات

---

## ✅ قائمة التحقق السريع

قبل الاستخدام، تأكد من:

- [ ] تم استيراد `complete_database.sql`
- [ ] بيانات الاتصال صحيحة في `includes/config.php`
- [ ] يمكن تسجيل الدخول للوحة التحكم
- [ ] السلايدرز تظهر في الصفحة الرئيسية
- [ ] يمكن إضافة أدمن جديد
- [ ] أزرار الواتساب تعمل
- [ ] الحجوزات تُحفظ في قاعدة البيانات

---

## 🎯 الملفات المهمة

- `complete_database.sql` - قاعدة البيانات الكاملة
- `check_and_fix_database.php` - أداة الفحص والإصلاح
- `includes/config.php` - إعدادات الاتصال بقاعدة البيانات
- `admin/login.php` - صفحة تسجيل الدخول
- `admin/users.php` - إدارة المستخدمين
- `admin/sliders.php` - إدارة السلايدرز
- `admin/bookings.php` - إدارة الحجوزات

---

**آخر تحديث:** ديسمبر 2025
**النسخة:** 2.0 (Unified Database)
